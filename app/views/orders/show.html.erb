<p id="notice"><%= notice %></p>
<p>
  <strong>Title:</strong>
  <%= best_in_place @order, :title %>
</p>

<p>
  <strong>Description:</strong>
  <%= best_in_place @order, :description %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @order.avatar.url(:thumb) %>
</p>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>

